{
  "name": "simaqian-skill",
  "version": "4.5.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "preview": "python3 -m http.server 8765",
    "export:setup": "playwright install chromium",
    "export:homepage": "node scripts/export-homepage.mjs"
  },
  "devDependencies": {
    "playwright": "^1.53.0"
  }
}
